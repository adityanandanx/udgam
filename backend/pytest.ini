[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Mark tests that require external API access
markers =
    api: Tests that make real API calls
    integration: Integration tests
    unit: Unit tests

# Verbose output and show local variables on failure
addopts = -v --showlocals
